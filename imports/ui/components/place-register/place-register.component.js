import template from './place-register.html';
import controller from './place-register.ctrl';

const PlaceRegisterComponent = {
  bindings: {},
  template,
  controller
};

export default PlaceRegisterComponent;
