import template from './type-icon.html';
import controller from './type-icon.ctrl';

const TypeIconComponent = {
  bindings: {
    value: '<'
  },
  template,
  controller
};

export default TypeIconComponent;
